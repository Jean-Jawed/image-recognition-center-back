# Image Recognition Center — Backend Dependencies
# Requires Python 3.11.x (TensorFlow/MediaPipe not compatible with 3.13+)

# Web framework
fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
websockets>=12.0,<13.0

# Computer vision
opencv-python-headless>=4.9.0,<5.0
numpy>=1.26.0,<2.0

# MediaPipe Tasks API (hands, pose, face mesh)
# Version 0.10.9+ includes Tasks API
# NOTE: mp.solutions is deprecated, we use mp.tasks instead
mediapipe>=0.10.9,<0.11

# Emotion detection
deepface>=0.0.89,<1.0
tensorflow>=2.15.0,<2.17

# Object detection
ultralytics>=8.1.0,<9.0

# Utilities
python-multipart>=0.0.9,<1.0